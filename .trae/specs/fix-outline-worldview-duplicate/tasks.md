# Tasks
- [x] Task 1: 修改 OutlinePage.tsx 中 handleSave 的世界观查找和保存逻辑
  - [x] 将 `worldSettings.find((w) => w.title === "worldview")` 改为 `worldSettings.find((w) => w.title === "世界观设定")`
  - [x] 将创建新条目时的 `title: "worldview"` 改为 `title: "世界观设定"`
